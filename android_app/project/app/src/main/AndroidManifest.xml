<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Needed to reach the device over HTTP. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- One-tap connect to the device hotspot (WifiNetworkSpecifier). -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="Glucose WiFi"
        android:supportsRtl="true"
        android:theme="@style/Theme.GlucoseWiFi"
        android:usesCleartextTraffic="true">

        <!-- usesCleartextTraffic is required: the device speaks plain http on
             192.168.4.1, not https. Android blocks cleartext by default. -->

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
